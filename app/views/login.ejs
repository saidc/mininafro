<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Iniciar sesión</title>
  <link rel="stylesheet" href="/public/css/login.css">
  
</head>
<body>
  <main class="login">
    <section class="login__card" aria-label="Inicio de sesión">
      <header class="login__header">
        <img class="login__logo" src="/public/img/vSpzn3D8_o.png" alt="Gobierno - Logo" />

        <h1 class="login__title">Ingreso al Sistema</h1>
        <p class="login__subtitle">Accede con tus credenciales institucionales</p>
      </header>

      <form method="POST" action="/login" class="login__form" id="loginForm" novalidate>
        <div class="field">
          <label class="field__label" for="username">Usuario</label>
          <input class="field__input" id="username" name="username" autocomplete="username" required
                 placeholder="usuario@entidad.gov.co" />
          <small class="field__hint" id="usernameHint">Ej: usuario@entidad.gov.co</small>
        </div>

        <div class="field">
          <label class="field__label" for="password">Contraseña</label>

          <div class="password">
            <input class="field__input password__input" id="password" name="password"
                   type="password" autocomplete="current-password" required placeholder="••••••••" />
            <button class="password__toggle" type="button" id="togglePassword"
                    aria-label="Mostrar u ocultar contraseña" aria-pressed="false">
              👁️
            </button>
          </div>

          <small class="field__hint">No compartas tu contraseña.</small>
        </div>

        <div class="login__actions">
          <button class="btn btn--primary" type="submit" id="btnLogin">
            Ingresar
          </button>
          
        </div>

        <div class="alert" id="loginAlert" role="alert" aria-live="polite" hidden></div>
      </form>

      <footer class="login__footer">
        <span>© <span id="year"></span> - Entidad / Gobierno</span>
      </footer>
    </section>
  </main>

  <script defer src="/public/js/login.js"></script>
</body>
</html>